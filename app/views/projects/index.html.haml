.col-sm-10
  %h4 Select a project to see its Kanban board

  .list-group
    - @projects.each do |project|
      %a.list-group-item.project-list-link{href: project_path(project.id)}
        = project.name

  %div.clear

